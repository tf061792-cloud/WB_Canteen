{
  "version": 2,
  "buildCommand": "cd admin && pnpm install && pnpm build",
  "outputDirectory": "admin/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
